doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 Noom 
        main
            div#welcome
                form
                    input(placeholder="Room name", required, type = "text")
                    button Enter Room
            div#room
                h3
                ul 
                form
                    input(placeholder="Message", required, type = "text")
                    button Send
            //- WebSocket
            //- form#nick
            //-     input(type="text", placeholder="choose a nickname", reqired)
            //-     button Save
            //- ul 
            //- form#message
            //-     input(type="text", placeholder="write a msg", reqired)
            //-     button Send
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")